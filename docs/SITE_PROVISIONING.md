# Site provisioning & activation — reference

**This doc has moved.** See
`bosca.shield/docs/Site Server Setup/SITE_PROVISIONING.md`.

It was relocated so the cross-repo provisioning reference lives alongside the
other Site Server Setup docs in `bosca.shield`.
